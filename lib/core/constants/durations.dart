/// Common durations
class Durations {
  static const Duration shortDuration = Duration(milliseconds: 500);
  static const Duration mediumDuration = Duration(milliseconds: 1000);
  static const Duration longDuration = Duration(milliseconds: 3000);
}
